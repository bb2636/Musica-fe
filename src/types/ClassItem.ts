export interface ClassItem {
  id: number;
  title: string;
  instructor: string;
  price: number;
  originalPrice?: number;
  rating: number;
  ratingCount: number;
  tag: string;
  thumbnailUrl: string;
}